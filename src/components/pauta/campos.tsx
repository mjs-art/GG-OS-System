/**
 * Los campos de formulario viven en `primitives.tsx` desde que una segunda
 * sección (la bandeja de WhatsApp) los necesitó. Este módulo se queda como
 * re-exporte para no tocar los imports que ya apuntaban aquí.
 */
export { Campo, Entrada, Selector, AreaTexto } from '@/components/ui/primitives'
